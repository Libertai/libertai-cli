# Stable since the alforria cutover — the pi_agent_rust nightly pin went
# with it (asupersync/fsqlite needed nightly; alforria and our own code do
# not). Match the MSRV of the release workflow.
[toolchain]
channel = "stable"
profile = "minimal"
components = ["rustfmt", "clippy"]
